{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ContactForm creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('contactform') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
