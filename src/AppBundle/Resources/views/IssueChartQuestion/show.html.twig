{% extends '::base.html.twig' %}

{% block body -%}
    <h1>IssueChartQuestion</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Charttype</th>
                <td>{{ entity.chartType }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('issuechartquestion') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('issuechartquestion_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
