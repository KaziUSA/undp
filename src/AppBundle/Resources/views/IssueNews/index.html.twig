{% extends '::base.html.twig' %}

{% block body -%}
    <h1>IssueNews list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>Imageurl</th>
                <th>Audiourl</th>
                <th>Youtubeurl</th>
                <th>Createddate</th>
                <th>Updateddate</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('issuenews_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.imageUrl }}</td>
                <td>{{ entity.audioUrl }}</td>
                <td>{{ entity.youtubeUrl }}</td>
                <td>{{ entity.createdDate }}</td>
                <td>{{ entity.updatedDate }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('issuenews_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('issuenews_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('issuenews_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
