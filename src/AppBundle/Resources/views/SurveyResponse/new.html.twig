{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SurveyResponse creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('surveyresponse') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
