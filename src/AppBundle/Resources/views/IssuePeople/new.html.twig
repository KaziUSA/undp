{% extends '::base.html.twig' %}

{% block body -%}
    <h1>IssuePeople creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('issuepeople') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
